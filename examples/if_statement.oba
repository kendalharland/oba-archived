// Output:
// DEBUG: true
// DEBUG: false
// DEBUG: the math is correct
// DEBUG: the math is incorrect

if true {
  debug "true"
}

if false {
  debug "true"
}

if false {
  debug "true"
} else {
  debug "false"
}

{ 
  let x = "the math is "
  if 10 * 100 - 5 == 995 {
    x = x + "correct"  
  } else {
    x = x + "incorrect"
  }
  debug x
}

{ 
  let x = "the math is "
  if 1 + 1 == 7 {
    x = x + "correct"  
  } else {
    x = x + "incorrect"
  }
  debug x
}
